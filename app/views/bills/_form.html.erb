<%= form_with(model: bill) do |form| %>
  <% if bill.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bill.errors.count, "error") %> prohibited this bill from being saved:</h2>

      <ul>
        <% bill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :radicado, style: "display: block" %>
    <%= form.text_field :radicado %>
  </div>

  <div>
    <%= form.label :SAO, style: "display: block" %>
    <%= form.text_field :SAO %>
  </div>

  <div>
    <%= form.label :numero_factura, style: "display: block" %>
    <%= form.text_field :numero_factura %>
  </div>

  <div>
    <%= form.label :fecha_llegada_recepcion, style: "display: block" %>
    <%= form.date_field :fecha_llegada_recepcion %>
  </div>

  <div>
    <%= form.label :tipo_proyecto, style: "display: block" %>
    <%= form.text_field :tipo_proyecto %>
  </div>

  <div>
    <%= form.label :fecha_entrega_compras, style: "display: block" %>
    <%= form.date_field :fecha_entrega_compras %>
  </div>

  <div>
    <%= form.label :fecha_entrega_sst, style: "display: block" %>
    <%= form.date_field :fecha_entrega_sst %>
  </div>

  <div>
    <%= form.label :fecha_entrega_gerencia, style: "display: block" %>
    <%= form.date_field :fecha_entrega_gerencia %>
  </div>

  <div>
    <%= form.label :fecha_entrega_contabilidad, style: "display: block" %>
    <%= form.date_field :fecha_entrega_contabilidad %>
  </div>

  <div class="field">
  <%= form.label :state_gerencia_id, "Estado Gerencia" %>
  <%= form.collection_select :state_gerencia_id, @gerencia_states, :id, :nombre %>
</div>

  <div class="field">
    <%= form.label :state_contabilidad_id, "Estado Contabilidad" %>
    <%= form.collection_select :state_contabilidad_id, @contabilidad_states, :id, :nombre %>
  </div>

  <div class="field">
    <%= form.label :state_sst_id, "Estado SST" %>
    <%= form.collection_select :state_sst_id, @sst_states, :id, :nombre %>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
