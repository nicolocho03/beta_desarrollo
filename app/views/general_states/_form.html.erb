<%= form_with(model: general_state) do |form| %>
  <% if general_state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(general_state.errors.count, "error") %> prohibited this general_state from being saved:</h2>

      <ul>
        <% general_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
